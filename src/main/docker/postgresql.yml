version: '2'
services:
    project-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/project/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=project
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
